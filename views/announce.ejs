<!DOCTYPE html>
<html>
  <head>
    <title>Manage</title>
    <meta charset="UTF-8">
  </head>
    <body>
        <%- include navbarAdmin %>

        <% if(data.length == 0){ %>
            <h1>ไม่พบข้อมูลให้จัดการ</h1>
        <% } %>
        <% for(var i=0; i<data.length; i++){ %>
            <%= data[i].title %>
            <%= data[i].author %>
            <%= data[i].content %>
        <% } %>
    </body>
</html>