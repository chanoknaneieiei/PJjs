<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Admin</title>
    <meta charset="UTF-8">
</head>
    <body>
      <%- include navbarAdmin %>
      <main class="container" style="margin-top:50px">
        <div class="card">
          <div class="card-header">
            <ul class="nav nav-pills w-100">
              <li class="nav-link">จัดการข้อมูล</li>
               <li class="nav nav-pills w-100">
            </ul>
          </div>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">รหัสนักศึกษา</th>
                <th scope="col">ชื่อ</th>
                <th scope="col">หมายเลขห้องพัก</th>
                <th scope="col">เบอร์โทร</th>
                <th scope="col">รายละเอียด</th>
                <th scope="col">การกระทำ</th> 
              </tr>
            </thead>
            <div class="card-body">
              <% if(data.length == 0){ %>
                <td>ไม่มีข้อมูลที่ได้รับแจ้ง</td>
              <% } %>
              <% for(var i=0; i<data.length; i++){ %>
                <tr>
                  <td><%= data[i].stid %></td>
                  <td><%= data[i].name %></td>
                  <td><%= data[i].room %></td>
                  <td><%= data[i].pnum %></td>
                  <td><%= data[i].problem %></td>
                  <td>
                    <form method="post" action="/roomAdmin/dataManage<%= data[i]._id %>">
                      <button class="btn btn-success delete" onclick="alert('Are you sure?')">ดำเนินการแล้ว</button>
                    </form>
                  </td>
                </tr>
              <% } %>
            </div>
          </table>
        </div>
      </main>
    </body>
</html>